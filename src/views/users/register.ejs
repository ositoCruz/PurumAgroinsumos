<!DOCTYPE html>
<html>
<%- include('../partials/head.ejs') %>
<body>
<%- include('../partials/header.ejs') %>
<link rel="stylesheet" href="../css/login.css" />
<main>
    <h1>Bienvenidos a Purum</h1>
<div class="login-container"> <!-- Cambiado de signin-container a login-container -->
    
    <form action="/users" method="post" enctype="multipart/form-data" class="form-register">
        <h2>Te invitamos a ser parte</h2>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="fullname">Nombre completo</label>
            <input type="text" id="fullname" name="fullname" placeholder="Nombre completo" value="<%= typeof old !== 'undefined' && old.fullname ? old.fullname : '' %>" class="input-error"> <!-- Agregada la clase input-error -->
            <br>
            <% if (locals.errors && errors.fullname){ %>
            <span class="form-validationError"> <%= errors.fullname.msg %> </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="username">Usuario</label>
            <input type="text" id="username" name="username" placeholder="Usuario" value="<%= typeof old !== 'undefined' && old.username ? old.username : '' %>" class="input-error"> <!-- Agregada la clase input-error -->
            <% if (locals.errors && errors.username){ %>
            <span class="form-validationError"> <%= errors.username.msg %> </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="email">Email</label>
            <div class="input-group">
                <input id="email" name="email" placeholder="Correo electrónico" value="<%= typeof old !== 'undefined' && old.email ? old.email : '' %>"> <!-- Agregada la clase input-error -->
            </div>
            <% if (locals.errors && errors.email){ %>
            <span class="form-validationError"> <%= errors.email.msg %> </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password" placeholder="Contraseña" class="input-error"> <!-- Agregada la clase input-error -->
            <% if (locals.errors && errors.password){ %>
            <span class="form-validationError"> <%= errors.password.msg %> </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="confirm_password">Confirmar contraseña</label>
            <input type="password" id="confirm_password" name="confirm_password" placeholder="Confirmar contraseña" class="input-error"> <!-- Agregada la clase input-error -->
            <% if (locals.errors && errors.confirm_password){ %>
            <span class="form-validationError"> 
                <%= errors.confirm_password.msg %>
             </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <label for="userImage">Imagen de perfil</label>
            <input type="file" id="userImage" name="userImage" accept="image/*"> <!-- Agregada la clase input-error -->
            <% if (locals.errors && errors.userImage){ %>
            <span class="form-validationError"> <%= errors.userImage.msg %> </span>
            <% } %>
        </div>
        <div class="login-item"> <!-- Cambiado de form-register-item a login-item -->
            <input type="submit" class="login-item button" value="Registrarme"> <!-- Agregada la clase button -->
        </div>
    </form>
    <br>
    <a href="/users/login">¿Ya tienes cuenta?</a>
</div>
</main>
<%- include('../partials/footer.ejs') %>
<script src="/js/registerValidations.js"></script>
</body>

</html>