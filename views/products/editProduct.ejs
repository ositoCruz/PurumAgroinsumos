<!DOCTYPE html>
<html lang="en">
    <%- include('../head.ejs') %>
<body>
    <%- include('../header.ejs') %>
    <main>
        
        <form action="/producto/editar/<%= product.id %>" method="post" enctype="multipart/form-data">
            <div class="div-form-create">
                <h2>EDITAR PRODUCTO</h2>
                <div>
                    <label for="name">Nombre</label>
                    <input type="text" placeholder="<%= product.name %>" value="" name="name" id="name" >
                </div>
                <div>
                    <label for="description">Descripcion</label>
                    <input type="text" placeholder="<%= product.description %>" name="description" id="description" value="">
                </div>
                <div>
                    <label for="category">Categoria</label>
                    <input type="text" placeholder="<%= product.category %>" name="category" id="category" value="">
                </div>
                <div>
                    <label for="price">Precio</label>
                    <input type="number" placeholder="<%= product.price %>" name="price" id="price" value="">
                </div>
                <div>
                    <label for="stock">Stock</label>
                    <input type="number" placeholder="<%= product.stock %>" name="stock" id="stock" value="">
                </div>
                <div>
                    <img src="/images/<%= product.image %>" alt="">
                </div>
                <div>
                    <label for="productImage">Seleccionar imagen:</label>
                    <input type="file" id="productImage" name="productImage" accept="image/*">
                </div>
                <div class="div-button-submit">
                    <button type="submit" class="btn-new-product">GUARDAR</button>
                </div>
            </div>
        </form> 
    </main>
    <%- include('../footer.ejs') %>
</body>
</html>